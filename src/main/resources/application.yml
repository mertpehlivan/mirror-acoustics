spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/mirroracoustics
    username: mirrorapp
    password: DEGISTIR-BUNU-COK-GUCLU
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false
  thymeleaf:
    cache: true
  web:
    locale-resolver: FIXED
    locale: tr
  servlet:
    multipart:
      max-file-size: 25MB
      max-request-size: 25MB

server:
  port: 8080
  forward-headers-strategy: NATIVE
  compression:
    enabled: true
    mime-types: text/html,text/xml,text/plain,text/css,application/json,application/javascript,image/svg+xml
    min-response-size: 1024

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint.health.probes.enabled: true

app:
  admin:
    username: admin
    default-password: admin123
  upload:
    dir: /opt/myapp/uploads
  showcase:
    image:
      width: 1500
      height: 1000
      quality: 0.9
  whatsapp:
    phone: "905327040949"

site:
  base-url: https://mirroracoustic.com
  brand: Mirror Acoustics Custom Audio Products
  locale-default: tr

logging:
  file.name: /opt/myapp/logs/app.log
  level.root: INFO
