spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/mirroracoustics
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  thymeleaf:
    cache: false

  messages:
    basename: messages

  web:
    locale: tr

  security:
    user:
      name: admin
      password: admin123
      roles: ADMIN
  servlet:
    multipart:
      max-file-size: 25MB
      max-request-size: 25MB

app:
  upload:
    dir: uploads
  whatsapp:
    phone: "905062515796"

site:
  base-url: https://www.mirroracoustics.example
  brand: Mirror Acoustics Custom Audio Products
  locale-default: tr


